# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# Runtime data — contains real emails, hashed passwords, and shift records.
# Never commit to version control. Restore from seed.js on a new deployment.
data/
# Exception: hotels.json is configuration (no PII), track it in version control.
!data/hotels.json
_tmp_build_users.js

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Logs
logs/
*.log

# Build artifacts
dist/
build/
coverage/
